{
  "devDependencies": {
    "autoprefixer": "^6.3.6",<% if (useES6) { %>
    "babel-preset-es2015": "^6.6.0",<% } %><% if (browserSync) { %>
    "browser-sync": "^2.11.2",<% } %>
    "cssnano": "^3.5.2",
    "gulp": "^3.9.1",<% if (useES6) { %>
    "gulp-babel": "^6.1.2",<% } %>
    "gulp-concat": "^2.6.0",
    "gulp-notify": "^2.2.0",
    <% if (usePostCSS) { %>"gulp-postcss": "^6.1.0",
    <% } else { %>"gulp-autoprefixer": "^3.1.0",
    "gulp-clean-css": "^2.0.8",<% } %>
    "gulp-sass": "^2.3.1",
    "gulp-sass-lint": "^1.1.1",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-uglify": "^1.5.3",
    "gulp-cache": "^0.4.5",
    <% if (useImagemin) { %>"gulp-imagemin": "^3.0.1",<% } %>
    <% if (usePostCSS) { %>"postcss-calc": "^5.2.1",
    "postcss-focus": "^1.0.0",
    "postcss-zindex": "^2.1.1",
    "postcss-flexbugs-fixes": "^2.0.0",
    "postcss-pxtorem": "^3.0.0",
    "postcss-import": "latest",
    "postcss-short": "^1.4.0",<% } %>
    <% if (useBourbon) { %>"bourbon": "latest",
    "node-neat": "latest",<% } %>
    <% if (useWebpack) { %>"babel-core": "latest",
    "babel-loader": "latest",
    "gulp-clean": "latest",
    "webpack": "latest",
    "webpack-stream": "latest",<% } %>
    <% if (useJQuery || useUIKit || useBootstrap) { %> "jquery": "latest",<% } %>
    <% if (useUIKit) { %>"uikit": "latest",<% } %>
    <% if (useBootstrap) { %>"bootstrap-sass": "latest",<% } %>
    "gulp-util": "^3.0.7"
  }
}
